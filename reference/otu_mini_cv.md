# Cross validation on `train_data_mini` with grouped features.

Cross validation on `train_data_mini` with grouped features.

## Usage

``` r
otu_mini_cv
```

## Format

An object of class `list` of length 27.

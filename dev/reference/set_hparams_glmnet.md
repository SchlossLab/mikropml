# Set hyperparameters for regression models for use with glmnet

Alpha is set to `0` for ridge (L2). An alpha of `1` would make it lasso
(L1).

## Usage

``` r
set_hparams_glmnet()
```

## Value

default lambda & alpha values

## Author

Zena Lapp, <zenalapp@umich.edu>
